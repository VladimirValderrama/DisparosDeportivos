import Options from 'transbank-sdk/dist/es5/transbank/common/options';
declare class BaseTransaction {
    options: Options;
    /**
     * Constructor class Webpay Plus transaction.
     * @param options You can pass options to use a custom configuration.
     */
    constructor(options: Options);
}
export default BaseTransaction;
